class ApiConstants {
  
  static const String baseUrl = 'Hugging face api url';
  static const String measureEndpoint = '$baseUrl/measure';
  static const String healthEndpoint = '$baseUrl/';
  static const Duration timeout = Duration(seconds: 60);
}
